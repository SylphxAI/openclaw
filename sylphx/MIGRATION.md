# Sylphx hosted OpenClaw

Sylphx Platform deploy artifacts (image, `sylphx.toml`, `agents.yaml`, runtime scripts)
live under `sylphx/` in this repository.

Previously: `SylphxAI/openclaw-deploy` (archived). Upstream OpenClaw application code
remains at the repo root; Sylphx-specific hosting boundaries stay in this directory.
